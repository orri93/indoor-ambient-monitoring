#ifndef CONVERT_H
#define CONVERT_H

// Temperature
float ctof(float c);          // Celsius to Fahrenheit

// Pressure
float patohpa(float pa);      // Pa to hPa
float patoatm(float pa);      // Pa to atm
float patombar(float pa);     // Pa to mbar
float patopsi(float pa);      // Pa to psi
float patoinhg(float pa);     // Pa to inHg
float patommhg(float pa);     // Pa to mmHg

#endif
